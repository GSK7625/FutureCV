/**
 * @file saved-jobs.tsx
 * @description Route trang việc làm đã lưu của ứng viên (/candidate/saved-jobs - P3-UC04).
 */
import { SavedJobsView } from "~/features/candidate/components/saved-jobs/SavedJobsView";

export default function CandidateSavedJobsPage() {
  return <SavedJobsView />;
}
