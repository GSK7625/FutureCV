/**
 * @file applications.tsx
 * @description Route danh sách hồ sơ việc làm đã ứng tuyển của ứng viên (/candidate/applications - FC-81b).
 */
import { ApplicationsView } from "~/features/candidate/components/applications/ApplicationsView";

export default function CandidateApplicationsPage() {
  return <ApplicationsView />;
}
